// For client-side authentication needs
export const authClient = {
  // The @neondatabase/auth SDK usually provides a client-side helper.
  // If not explicitly provided in the snippet, we can implement wrappers
  // or use the server-side actions.
};
